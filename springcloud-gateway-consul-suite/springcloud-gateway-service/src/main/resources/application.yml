spring:
  application:
    name: spring-cloud-gateway-service
  cloud:
    consul:
      discovery:
        register: true
      host: localhost
      port: 8500
    gateway:
      routes:
        - id: service-router
          uri: lb://service-provider
          predicates:
            - Path=/service/*

server:
  port: 9091

